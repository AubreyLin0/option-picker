import { Card, CardBody, CardHeader, Divider } from "@chakra-ui/react";

const Option = () => {
  return (
    <Card>
      <CardHeader>Test Head</CardHeader>
      <Divider color={"gray.300"} />
      <CardBody>Test Body</CardBody>
    </Card>
  );
};

export default Option;
